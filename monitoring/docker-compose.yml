# NOTE: This compose file uses network_mode: host which only works on Linux.
# For macOS/Windows (Docker Desktop), use the override file:
#   docker compose -f docker-compose.yml -f docker-compose.macos.yml up -d
# The override replaces network_mode with explicit port mappings and a bridge network.

services:
  # Loki (log aggregation)
  loki:
    image: grafana/loki:3.4.2
    container_name: loki
    restart: unless-stopped
    network_mode: host
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./loki-config.yaml:/etc/loki/local-config.yaml:ro
      - loki_data:/loki

  # OpenTelemetry Collector
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: otel-collector
    restart: unless-stopped
    command: ["--config=/etc/otel-collector-config.yaml"]
    network_mode: host
    volumes:
      - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
      - ${HOME}/.claude/.statusline/events.jsonl:/var/log/claude/events.jsonl:ro
    depends_on:
      - prometheus
      - loki

  # Prometheus
  prometheus:
    image: docker.io/prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'

  # Node Exporter (textfile collector for warden budget metrics)
  node-exporter:
    image: docker.io/prom/node-exporter:latest
    container_name: node-exporter
    restart: unless-stopped
    network_mode: host
    command:
      - '--collector.textfile.directory=/textfile'
      - '--collector.disable-defaults'
      - '--collector.textfile'
      - '--web.listen-address=:9101'
    volumes:
      - ${HOME}/.claude/.monitoring/textfile:/textfile:ro

  # Grafana
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    network_mode: host
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_SECURITY_ADMIN_USER=admin
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus

  # Tempo (distributed tracing)
  tempo:
    image: grafana/tempo:2.7.2
    container_name: warden-tempo
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./tempo-config.yaml:/etc/tempo.yaml:ro
      - tempo_data:/var/tempo
    command: ["-config.file=/etc/tempo.yaml"]

volumes:
  prometheus_data:
  grafana_data:
  loki_data:
  tempo_data:
